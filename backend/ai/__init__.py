"""
TraVerse AI infrastructure package.

This package contains framework-independent AI components shared across
the platform.

The package intentionally contains zero Django dependencies so it can
be reused, independently tested, and potentially extracted into a
standalone service in the future.
"""